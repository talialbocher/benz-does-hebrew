# תהליך יצירת שיעור חדש — מדריך עבודה

המטרה: שיעורים עקביים, נכונים פדגוגית וללא שגיאות, שמעבירים את בן **בהדרגה
לרמת כיתה ה׳ מלאה**, עם ניקוד מלא ונכון בכל שיעור (ראו סעיף 3 למטה).
העיקרון: **שלבים נפרדים, כל שלב עם "עיניים טריות"** — לא סוכן לכל קטע בשיעור,
אלא סוכן יוצר אחד + סוכן מגיה נפרד + בדיקה אוטומטית.

## סדרת כיתה ה׳ (החל מעכשיו)

שיעורים חדשים נשמרים בתיקיית `5thGrade/`, בהמשך למספור הקיים שם
(`lesson01`, `lesson02` כבר קיימים — הבא הוא `lesson03`).

ארבעה שינויים קבועים לעומת הסדרה הקודמת:

1. **רמת כיתה ה׳** — טקסטים ארוכים ועשירים יותר, שאלות הסקה ופרשנות לצד
   שליפה, ואוצר מילים ברמת כיתה ה׳.
2. **קטע הקריאה = יצירה מוכרת, עם גיוון וכלל רוטציה** — כל שיעור נבנה סביב
   יצירה קצרה מפורסמת. **סדר עדיפות:**
   1. **שיר מוכר בנחלת הכלל** (יוצר/ת שנפטר/ה לפני 70+ שנים — ביאליק, רחל,
      טשרניחובסקי ואחרים) — **רק אם יש טקסט מאומת** במאגר בחלק ג ב-`curriculum.md`
      (או שהסוכן מצליח לאמת טקסט מדויק ממקור אמין באותה הרצה). לצטט במדויק
      ולציין שם היוצר/ת.
   2. **סיפור עם מוכר מרחבי העולם** (איזופוס, אחי גרים, אנדרסן, אלף לילה
      ולילה, סיפורי חכמי חלם, אגדות מאסיה/אפריקה/אמריקה הלטינית וכו') —
      מעובד/מסופר מחדש, לא ציטוט מדויק. **ברירת המחדל** כשאין טקסט שיר מאומת
      (ראו סעיף 4 למטה) — עדיף על אגדת חז"ל נוספת.
   3. **אגדת חז"ל / סיפור תנ"כי מעובד** — **בשימוש מדוד בלבד: לכל היותר שיעור
      אחד מכל שלושה שיעורים רצופים** בסדרת כיתה ה׳. לפני שבוחרים אגדת חז"ל,
      לבדוק בטבלה בחלק א2 ב-`curriculum.md` מה היו סוגי קטעי הקריאה בשני
      השיעורים הקודמים בסדרה — אם אחד מהם כבר היה אגדת חז"ל/סיפור תנ"כי,
      **אסור** לבחור שוב אגדת חז"ל והשיעור הזה חייב להיות שיר או סיפור עם.
   4. **אם השיר הרצוי אין לו טקסט מאומת** (הסוכן לא בטוח בניסוח המדויק,
      ואין גישה לאימות באינטרנט באותה הרצה) — **אין לנחש את הטקסט ואין
      להחליף באגדת חז"ל כברירת מחדל**. לעבור לסיפור עם מוכר (סעיף 2), או
      לבחור שיר אחר מהמאגר שיש לו טקסט מאומת.

   מאגר מומלץ מלא (שירים עם סטטוס אימות, וסיפורי עם מוכנים לשימוש): חלק ג
   ב-`curriculum.md`. **המאגר ממתין לבדיקה חד-פעמית של טלי** — ראו הערה
   בראש חלק ג. ליצירה שעדיין מוגנת בזכויות יוצרים — קטע קצר בלבד, לא ציטוט מלא.
3. **ניקוד מלא בכל שיעור, ללא יוצא מן הכלל** — כל קטע הקריאה (וכל טקסט עברי
   אחר בשיעור: אוצר מילים, שאלות, כללי דקדוק, הוראות) מנוקד במלואו, מהשיעור
   הראשון בסדרה ואילך. **אין מדיניות מדורגת ואין מעבר לקריאה ללא ניקוד** —
   זו הייתה המדיניות הישנה והיא בוטלה.
   - הניקוד חייב להיות נכון דקדוקית ומלא בכל מילה שמופיעה בטקסט.
   - כשמנקדים טקסט קיים (למשל בעדכון שיעור ישן) — מוסיפים ניקוד בלבד ואסור
     לשנות אותיות/כתיב, כולל שמירה על "כתיב מלא" לא-רשמי אם כך הופיע במקור.
4. **איור אחד לפחות לכל שיעור** — מוטמע בתוך קטע הקריאה (`.story-box`), בערך
   באמצע הסיפור, בין שתי פסקאות. סדר עדיפות:
   1. **תמונה אמיתית בנחלת הכלל** — לחפש (WebSearch) תמונה מתאימה בוויקישיתוף
      (Wikimedia Commons). להשתמש רק בקובץ אמיתי שנמצא בפועל בחיפוש —
      **אסור לעולם להמציא/לנחש שם קובץ**. לבדוק שהיא באמת נחלת הכלל: יוצר/ת
      שנפטר/ה לפני 70+ שנה, תיעוד PD מפורש בדף הקובץ, או יצירה בת 100+ שנה
      בעלת שם קובץ עצמאי (למשל שם היוצר/ת + שם היצירה + שנה) — **לא** יצירה
      מודרנית ברישיון CC (כמו CC BY-SA) של יוצר/ת חי/ה או שנפטר/ה לאחרונה.
      להטמיע ישירות דרך מנגנון `Special:FilePath`:
      ```html
      <figure class="story-illus">
        <img src="https://commons.wikimedia.org/wiki/Special:FilePath/EXACT_FILENAME.jpg"
             alt="תיאור מנוקד" loading="lazy">
        <figcaption>כיתוב מנוקד</figcaption>
        <div class="credit">שם היוצר/ת ושנה, נחלת הכלל (Wikimedia Commons)</div>
      </figure>
      ```
      אין צורך בביטחון גבוה מדי לפני הטמעה — מספיק קובץ אמיתי וסביר; טלי
      מוודאת ויזואלית אחר כך. עדיף קובץ אמיתי בינוני על ויתור.
   2. **איור SVG מקורי** — כשלא נמצא קובץ אמיתי מתאים (או שנמצאה רק יצירה לא
      חופשית) — לצייר איור מקורי בהתאם לפלטת הצבעים של השיעור:
      ```html
      <figure class="story-illus">
        <svg viewBox="0 0 600 200" role="img" aria-label="תיאור מנוקד">...</svg>
        <figcaption>כיתוב מנוקד</figcaption>
      </figure>
      ```
      שימו לב לבאג נפוץ: תבנית `<g id="...">` שמיועדת לשימוש חוזר דרך
      `<use href="#...">` **חייבת** להיות עטופה ב-`<defs>...</defs>`, אחרת
      היא גם מצטיירת פעם נוספת במקומה המקורי (0,0) ויוצרת פסולת ויזואלית
      בפינה השמאלית-עליונה של האיור.
   - ה-CSS הדרוש (`.story-illus` וכו') כבר קיים ב-`lesson_template.html`.
   - שני האיורים גם יחד לעולם אינם מומצאים: תמונה אמיתית = קובץ אמיתי שנמצא
     בחיפוש; SVG = ציור מקורי. לעולם לא "תמונה אמיתית" מזויפת.

## קבצי המערכת (בתיקייה `_system`)

| קובץ | תפקיד |
|------|--------|
| `curriculum.md` | מפת הנושאים — מה כוסה ומה הבא בתור |
| `ben_progress.md` | מעקב אישי — איפה בן חזק ואיפה צריך חיזוק |
| `lesson_template.html` | תבנית קבועה: עיצוב, מבנה, מנוע משחקים (MC), מטלת כתיבה |
| `games_reference.md` | מדריך יישום לסוגי משחק נוספים: תפזורת, תשבץ |
| `proofread_checklist.md` | צ׳ק-ליסט לסוכן ההגהה |
| `verify_lesson.py` | בדיקה טכנית אוטומטית |

## השלבים

### שלב 1 — בחירת נושא (דקה)
פתחו את `curriculum.md` ואת `ben_progress.md`. בחרו:
- נושא חדש מחלק ב של תכנית הלימודים, **או**
- שיעור חזרה לנושא שמסומן 🔁 (דורש חיזוק).
כלל אצבע: על כל 3–4 נושאים חדשים — שיעור חזרה אחד.

### שלב 2 — יצירת השיעור (סוכן יוצר)
פרומפט מוכן (להעתיק לשיחה חדשה / לסוכן):

> צור שיעור עברית לכיתה ה׳ מספר NN בנושא [נושא] לפי התבנית
> `_system/lesson_template.html`. קרא קודם את `_system/curriculum.md`
> (להקשר ולרצף) ואת השיעור הקודם בתיקיית `5thGrade/` (לקישורי הניווט).
> דרישות: קטע הקריאה הוא שיר או סיפור קצר מפורסם (עדיפות לנחלת הכלל —
> ראה חלק ג ב-curriculum.md), מצוטט במדויק עם שם היוצר/ת, וכל השיעור
> נבנה סביבו; **ניקוד מלא ונכון בכל הטקסט העברי בשיעור** (קטע הקריאה,
> אוצר המילים, השאלות, כללי הדקדוק) — ללא יוצא מן הכלל; **איור אחד לפחות
> בתוך קטע הקריאה** — לחפש תמונה אמיתית בנחלת הכלל בוויקישיתוף (קובץ אמיתי
> בלבד, לעולם לא להמציא שם קובץ), ואם לא נמצאה — לצייר SVG מקורי (ראו סעיף 4
> ב-WORKFLOW.md לפירוט מלא); פלטת צבעים ייחודית; סצנה ויזואלית מקורית לכל
> משחק (דרך onCorrectComp/onCorrectGram); קטע כתיבה אחד;
> 5–6 שאלות הבנה (כולל שאלות הסקה) + 6–8 שאלות דקדוק, **לפי כלל אורך
> המסיחים למטה** — ארבע האפשרויות באותו אורך בערך, והתשובה הנכונה אינה
> הארוכה או המפורטת ביותר; אסור להשאיר
> {{placeholders}}. לגיוון: בדקו אם השיעור הזה מתאים לתפזורת או תשבץ
> באחד מחלקי המשחק במקום MC (ראו `_system/games_reference.md` לדוגמאות
> עובדות ולכלל האצבע לתדירות). שמור כ-`5thGrade/lessonNN_shem_beanglit.html`.

חשוב: הסוכן היוצר כותב הכל יחד — היצירה המצוטטת מכתיבה את אוצר המילים, השאלות והמשחקים.

### שלב 3 — הגהה (סוכן נפרד, קונטקסט נקי!)
לפתוח שיחה חדשה או תת-סוכן שלא ראה את תהליך היצירה:

> אתה מגיה מומחה לעברית ולניקוד. עבור על הקובץ `5thGrade/lessonNN_*.html`
> לפי הצ׳ק-ליסט `_system/proofread_checklist.md`. בדוק **כל** טקסט עברי,
> כולל מחרוזות בתוך ה-JavaScript. תקן ישירות בקובץ ודווח על כל תיקון.
> שים לב במיוחד לנכונות פדגוגית: שכל עובדה לשונית נכונה ושכל תשובה
> מסומנת-כנכונה אכן נכונה; שהציטוט מהיצירה המוכרת מדויק ושם היוצר/ת
> מצוין; **ושהניקוד מלא בכל הטקסט העברי בשיעור ונכון בכל מילה** — שיעור
> עם קטעים לא-מנוקדים נחשב לא גמור ויש להשלים בו ניקוד; **ושיש בשיעור איור
> אחד לפחות** בתוך קטע הקריאה — אם קיים `<img>` לוויקישיתוף, לוודא שהכיתוב
> וה-alt מנוקדים ושיש קרדיט; אם קיים SVG, לוודא שאין פסולת ויזואלית מ-`<g>`
> לא עטוף ב-`<defs>`; אם אין איור בכלל — לדווח שהשיעור לא גמור.

למה סוכן נפרד? היוצר "עיוור" לשגיאות של עצמו. בסבב התיקונים של שיעורים
49–59 נמצאו עשרות שגיאות שעברו את היוצר — כולל תוכן פדגוגי שגוי.

### שלב 4 — בדיקה אוטומטית
```
python3 "_system/verify_lesson.py" 5thGrade/lessonNN_*.html
```
בודק: HTML תקין, JavaScript תקין, קטע כתיבה אחד, עיצוב הסדרה, ניגודיות
צבעים, שגיאות עברית מוכרות, מספור אחיד, קישורי ניווט, placeholders שנשכחו.
אם נמצאו בעיות — לתקן ולהריץ שוב עד ✅.

### שלב 5 — עדכון רישומים (דקה)
1. הוסיפו את השיעור לטבלה ב-`curriculum.md` וסמנו את הנושא ככוסה.
2. אחרי שבן עושה את השיעור — עדכנו את `ben_progress.md`:
   ענה נכון ברוב השאלות לבד → ✅ · התקשה → 🔁 + הערה ביומן.

### שלב 6 — שמירה ב-GitHub (חובה!)
לאחר ✅ בשלב 4 ועדכון הרישומים בשלב 5, יש לשמור את השיעור בריפו:
```
git add 5thGrade/lessonNN_*.html _system/curriculum.md _system/ben_progress.md
git commit -m "Add lessonNN: [נושא]"
git push
```
אין לסיים את תהליך יצירת השיעור בלי commit + push. אם ה-push נכשל
(למשל נעילת .git/index.lock מ-GitHub Desktop פתוח) — לבדוק אם יש תהליך git
פעיל בפועל לפני הסרת הנעילה, ולנסות שוב.

## עקרונות פדגוגיים קבועים

- שפה ברמת כיתה ה׳: טקסטים עשירים יותר, 6–10 מילים חדשות לשיעור.
- **כלל אורך המסיחים — התשובה הנכונה לא "מסגירה את עצמה":** זו הייתה
  הבעיה הנפוצה ביותר בשיעורים 3–26, במיוחד במשחק הבנת הנקרא. הנטייה
  הטבעית היא לנסח את התשובה הנכונה כמשפט מלא ומדויק, ואת שלושת המסיחים
  כמילה-שתיים — וכך בן יכול לסמן את התשובה הארוכה ביותר ולצבור 6/6 בלי
  לקרוא את הסיפור בכלל. הכללים:
  1. **אורך אחיד** — ארבע האפשרויות באותו טווח אורך, הפרש של עד ~3 מילים
     בין הקצרה לארוכה. אם התשובה הנכונה יצאה ארוכה: או לקצר אותה, או
     להאריך את שלושת המסיחים לאותה רמת פירוט.
  2. **התשובה הנכונה לא תמיד הארוכה** — בחלק מהשאלות היא צריכה להיות
     דווקא מהקצרות שבארבע.
  3. **מסיח טוב נשמע סביר למי שלא קרא** — פרט אמיתי מהסיפור בהקשר שגוי,
     סיבה הגיונית שאינה זו שבטקסט, או ניסוח שמערבב שתי דמויות. לא מסיח
     אבסורדי או מצחיק שאפשר לפסול במבט.
  4. **גיוון מיקום** — לא לשים את `ans:0` בכל השאלות. (מנוע התבנית מערבב
     את האפשרויות בכל טעינה דרך `shuffleOptions`, אבל אין להסתמך על זה
     במקום לנסח מסיחים טובים — ערבוב לא מתקן מסיח קצר ובולט.)

  `verify_lesson.py` בודק את זה אוטומטית ונכשל אם התשובה הנכונה ארוכה
  בהרבה מכל המסיחים, או אם היא האפשרות הארוכה ביותר ברוב השאלות.
- כל כלל דקדוק מנוסח בפשטות + טבלה + דוגמאות + יוצאי דופן נפוצים.
- חזרה מרווחת: נושא נלמד → חיזוק אחרי 10–15 שיעורים בטקסט והקשר חדשים.
- משחק שונה בכל שיעור (לפי הנחיות הפרויקט) — המנוע קבוע, הסצנה מתחלפת.
  לצד מנוע ה-MC (ברירת המחדל), **לגוון גם עם תפזורת ותשבץ** (מדריך יישום
  מלא, כולל דוגמאות עובדות: `games_reference.md`) — כלל אצבע: לפחות אחד
  מכל 4–5 שיעורים ישתמש בתפזורת או בתשבץ בחלק ג או בחלק ה, במקום MC.
- קטע הקריאה בכל שיעור: עדיפות לשיר מוכר בנחלת הכלל עם טקסט מאומת, אחר כך
  סיפור עם מוכר מרחבי העולם; אגדת חז"ל/סיפור תנ"כי לכל היותר שיעור אחד
  מכל שלושה (ראו כלל הרוטציה וסדר העדיפות בסעיף 2 למעלה, ומאגר מלא בחלק ג
  ב-curriculum.md). ציטוט משיר — במדויק, עם קרדיט לשם היוצר/ת.
- ניקוד: מלא בכל שיעור, בכל הטקסט העברי, ללא יוצא מן הכלל (ראו סעיף 3 למעלה).
- איור: אחד לפחות בכל שיעור, בתוך קטע הקריאה — עדיפות לתמונה אמיתית בנחלת
  הכלל (קובץ אמיתי בלבד, לעולם לא מומצא), אחרת SVG מקורי (ראו סעיף 4 למעלה).
